<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<aside>
  <div><h1></h1></div>
</aside>

<!-- if (post.author.equals(user?.profile._id)) { -->
<main>
  
<% if (user) { %>
<form  action="/posts" method="POST" >
    <label for="post-textarea"></label>
    <textarea type= "text" name="post" id="post-textarea" cols="70" rows="7" maxlength="150"></textarea>
    <button class="btn" type="submit">talk</button>
</form>
<% } %>

  <h1 class="logo">see what they are talking about</h1>
  <!-- <ul> -->
  <% posts.forEach(post => { %>
    <div class="profile-post">
    <h4> <%= post.author.name %></h4>
    <img src="<%= post.author.avatar %>" alt="<%= post.author.name %> avatar"/>
    <div class="post"> <%= post.post %> </div>
    <form action="/posts/<%= post._id %>"><button class="btn">Details</button></form>
    <% if(post.author.equals(user?.profile._id)) { %>
    <form action="/posts/<%= post._id %>?_method=DELETE" method="POST">
      <button class="delete-btn">Delete</button>
    </form>
  </div>
    <% } %>

  <% }) %>
<!-- </ul> -->
</main>

<article><div><h1></h1></div></article>

<%- include('../partials/footer') %>